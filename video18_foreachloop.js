const days=['mon','tues','wed','thurs','fri','sat']

console.log(days[0])

days.forEach(function(tiger,index){
    console.log(`day start with ${index+1}: ${tiger}`)
})



//in the function the first element is the element of that array
//the second element is the array index
//here the tiger is the dummy variable